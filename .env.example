# Hyperdrive Auditor has no required runtime environment variables.
# Host repositories should maintain their own .env.example files for audited apps.
# Set to 1 only in Hyperdrive internal tests to exercise the regex fallback graph.
HYPERDRIVE_DISABLE_TYPESCRIPT=
